# Runtime deps for the cloud deploy (pinned to the dev machine's working versions).
# Loosen to >= if a wheel is unavailable for your Python on the VM.
dash==4.2.0
dash-bootstrap-components==2.0.4
plotly>=5.0
Flask==3.1.3
fyers-apiv3==3.1.12
pandas==3.0.3
numpy==2.4.5
pyarrow==24.0.0
duckdb==1.5.3
pandas-ta==0.4.71b0
requests==2.31.0
