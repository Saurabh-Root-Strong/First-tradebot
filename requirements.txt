# Runtime deps for the cloud deploy (pinned to the dev machine's working versions).
# Loosen to >= if a wheel is unavailable for your Python on the VM.
dash==4.2.0
dash-bootstrap-components==2.0.4
plotly>=5.0
Flask==3.1.3
fyers-apiv3==3.1.12
pandas==3.0.3
numpy==2.2.6   # capped <2.3 so numba (via pandas-ta) resolves on Linux/py3.12; dev box (py3.14) has no numba so used 2.4.5
pyarrow==24.0.0
duckdb==1.5.3
pandas-ta==0.4.71b0
requests==2.31.0
yfinance==1.5.1   # global macro feeds (Brent/USD-INR/DXY/yields/US-futures/metals) — Fyers serves none
